name: redis-stack
services:
  redis-stack:
    image: redis/redis-stack:latest
    container_name: redis-stack
    ports:
      - "8009:6379"   
      - "8001:8001"
    volumes:
        - redis_data:/data
    restart: unless-stopped
volumes:
    redis_data: